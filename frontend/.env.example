# 前端开发环境变量示例
# 复制为 .env 后按需修改；.env 已被 .gitignore 忽略。

# 开发服务器 (vite dev, 5173) 将 /api 与 /ws 反向代理到的后端地址。
# Python 后端默认运行在 8000 端口。
VITE_API_PROXY_TARGET=http://localhost:8000

# 可选：请求的基础 URL 前缀（留空表示与页面同源，经 vite 代理转发）。
# VITE_API_BASE_URL=
